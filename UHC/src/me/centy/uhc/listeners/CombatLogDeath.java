package me.centy.uhc.listeners;

import org.bukkit.event.Listener;

public class CombatLogDeath implements Listener {

	/*
	 * @EventHandler public void onNPCDeath(EntityDeathEvent event) { Player killer
	 * = event.getEntity().getKiller(); Player loggedPlayer =
	 * Bukkit.getPlayer(event.getEntity().getCustomName()); if
	 * (event.getEntityType() == EntityType.VILLAGER &&
	 * event.getEntity().getCustomName() != null) {
	 * Bukkit.broadcastMessage(ChatColor.GREEN + killer.getDisplayName() +
	 * ChatColor.WHITE + " has killed " + ChatColor.GREEN +
	 * loggedPlayer.getDisplayName() + ChatColor.WHITE + "'s Combat Logger");
	 * GameManager.getInstance().removePlayer(loggedPlayer.getUniqueId()); } }
	 */

}